# Core (no GPU needed)
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# HuggingFace stack
torch>=2.1.0
transformers>=4.45.0
datasets>=2.20.0
accelerate>=0.30.0
peft>=0.11.0

# TRL — GRPOTrainer landed in 0.9; 0.12+ recommended
trl>=0.12.0

# QLoRA quantization (optional — only needed when use_4bit=true)
bitsandbytes>=0.43.0

# Logging
wandb>=0.17.0
